# Python-generated files (anchored to repo root; otherwise `dist/` would
# also match frontend/dist/, which langgraph deploy needs to ship).
__pycache__/
*.py[oc]
/build/
/dist/
/wheels/
*.egg-info

# Virtual environments
.venv

.env

# LangGraph CLI runtime state
.langgraph_api/
